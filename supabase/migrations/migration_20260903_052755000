-- Allow admins to permanently delete/drop a case report (distinct from closing it)
CREATE POLICY "admins_delete_case_reports"
  ON case_reports FOR DELETE TO authenticated
  USING (EXISTS (
    SELECT 1 FROM profiles WHERE id = auth.uid() AND (is_admin = true OR is_super_admin = true)
  ));
